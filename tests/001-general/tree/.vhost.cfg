Port 80
NginxPort 80
Depth 2

VirtualHost <<<<
<VirtualHost *[:{port}]>
    ServerName {domain}
[    ServerAlias {alias}
]    DocumentRoot "{doc_root}"
    ErrorLog "{cwd}/logs/error/{domain}.log"
[{extheaders}
]    <Directory "{doc_root}">
        AllowOverride All
    </Directory>
</VirtualHost>

>>>>

NginxServer <<<<
{!v2}
server {
{?    listen {:nginxport:} ;
?}    server_name {:domain:} {:alias:} ;
    root "{:doc_root:}" ;
{:extheaders:}
    location / {
        # ...
    }
}

>>>>
